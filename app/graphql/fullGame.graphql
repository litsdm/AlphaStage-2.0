query FullGame($id: String) {
  game(_id: $id) {
    coverImage,
    descriptionState,
    developerIds,
    genre,
    macBuild,
    releaseStatus,
    screenshots,
    shortDescription,
    tags,
    title,
    thumbnail,
    trailer,
    windowsBuild,
  }
}
