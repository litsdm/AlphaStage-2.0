query FullGame($id: String) {
  game(_id: $id) {
    _id,
    buildsId,
    coverImage,
    descriptionState,
    developerIds,
    genre,
    isPrivate,
    languages,
    macBuild,
    publisher,
    releaseStatus,
    screenshots,
    shortDescription,
    spaceRequired,
    tags,
    testingSessions {
      startDate,
      endDate
    }
    title,
    thumbnail,
    trailer,
    website,
    windowsBuild,
  }
}
