query FullGame($id: String) {
  game(_id: $id) {
    _id,
    buildsId,
    coverImage,
    descriptionState,
    developerIds,
    genre,
    isPrivate,
    languages,
    macBuild,
    publisher,
    releaseStatus,
    screenshots,
    shortDescription,
    spaceRequired,
    tags,
    testingSessions {
      _id,
      startDate,
      endDate,
      objectives
    }
    title,
    thumbnail,
    trailer,
    website,
    windowsBuild,
  }
}
