query FullGame($id: String) {
  game(_id: $id) {
    _id,
    coverImage,
    descriptionState,
    developerIds,
    genre,
    macBuild,
    releaseStatus,
    screenshots,
    shortDescription,
    tags,
    title,
    thumbnail,
    trailer,
    windowsBuild,
  }
}
