query UserGames($id: String) {
  user(_id: $id) {
    games {
      _id,
      coverImage,
      developerIds,
      downloads,
      pageViews,
      plays,
      title,
      thumbnail,
      uninstalls
    }
  }
}
